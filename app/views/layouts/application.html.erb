<!DOCTYPE html>
<%= airbrake_javascript_notifier %>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pesquisa Docente</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>
    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span9">
              <% if flash[:erro] %>
               <div class="alert alert-error">
                 <a class="close" data-dismiss="alert">×</a>
                 <strong>Erro!</strong> <%= flash[:erro] %>
               </div>
              <% end %>
              
              <% if flash[:notice] %>
               <div class="alert alert-info">
                 <a class="close" data-dismiss="alert">×</a>
                 <strong>Atenção!</strong> <%= flash[:notice] %>
               </div>
              <% end %>
              
              <% if flash[:alert] %>
               <div class="alert">
                 <a class="close" data-dismiss="alert">×</a>
                 <strong>Atenção!</strong> <%= flash[:alert] %>
               </div>
              <% end %>

              <% if flash[:atencao] %>
               <div class="alert">
                 <a class="close" data-dismiss="alert">×</a>
                 <strong>Atenção!</strong> <%= flash[:atencao] %>
               </div>
              <% end %>

              <% if flash[:sucesso] %>
               <div class="alert alert-success">
                 <a class="close" data-dismiss="alert">×</a>
                 <strong>Parabéns!</strong> <%= flash[:sucesso] %>
               </div>
              <% end %>
              <%= yield %>
            </div>
            <div class="span3">
              <%= yield(:sidebar) %>
            </div><!--/span-->
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; LTI - Laboratório de Tecnologia da Informação 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield(:scripts) -%>

  </body>
</html>
